---

host_keys: "{{ secrets_host_keys }}"

# This adds in a jump host
ansible_ssh_extra_args: "{{ secrets_ssh_args }}"

openrc_file_contents: "{{ secrets_openrc_file_contents }}"

tempest_config_contents: |-
  # BEGIN ANSIBLE MANAGED BLOCK
  # WARNING: This file was generated using shakespeare.
  # See: https://github.com/stackhpc/shakespeare
  # END ANSIBLE MANAGED BLOCK
  [DEFAULT]
  openstack_client_http_timeout = 300

  [auth]
  compute_quotas = cores:-1,ram:-1

  [identity]
  v3_endpoint_type = publicURL

  [compute]
  min_microversion = 2.1
  max_microversion = 2.65

  [service-clients]
  http_timeout = 600

  [compute-feature-enabled]
  resize = true
  console_output = true
  live_migration = true
